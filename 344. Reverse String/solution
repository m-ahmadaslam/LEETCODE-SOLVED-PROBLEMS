class Solution(object):
    def reverseString(self, s):
        left, right=0, len(s)-1 #left pointer is at start , right is at end

        while left<right: #while condition untill both char. meets
            s[left],s[right]=s[right],s[left] #this moves character 
            left+=1 
            right-=1

       
